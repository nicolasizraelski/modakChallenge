export interface IProduct {
  id: string;
  title: string;
  price: number;
  thumbnail: string;
  rating: number;
  brand: string;
  description: string;
  stock: number;
  category: string;
  discountPercentage: number;
  reviewAmount: number;
  availabilityStatus: string;
}
